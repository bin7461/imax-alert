name: CGV IMAX Booking Watcher

on:
  schedule:
    - cron: "*/5 * * * *"   # 5분마다 실행 (GitHub 인프라 상황에 따라 지연될 수 있음)
  workflow_dispatch:         # Actions 탭에서 수동 실행용 버튼

permissions:
  contents: write            # state.json 커밋을 위해 필요

concurrency:
  group: cgv-imax-watcher
  cancel-in-progress: false

jobs:
  check:
    # GitHub가 제공하는 ubuntu-latest(해외 데이터센터 IP)로는 CGV가 요청을 차단해
    # System_Notice.html로 리다이렉트시킨다. 국내 IP를 쓰는 self-hosted runner에서
    # 실행해야 한다. (README의 "문제 해결" 항목 참고)
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Check CGV IMAX booking status
        env:
          DOORAY_WEBHOOK_URL: ${{ secrets.DOORAY_WEBHOOK_URL }}
          CUSTOM_THEATER_CD: ${{ secrets.CUSTOM_THEATER_CD }}   # THEATER_CODES에 없는 극장일 때만 필요
          MOVIE_NAME: ${{ vars.MOVIE_NAME }}
          THEATER: ${{ vars.THEATER }}
          HALL_TYPE: ${{ vars.HALL_TYPE }}
        run: python check_cgv_imax.py

      - name: Commit updated state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/state.json
          git diff --cached --quiet || git commit -m "chore: update booking state [skip ci]"
          git push
